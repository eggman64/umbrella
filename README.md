umbrella
========

Book sharing project using openlibrary.

Installation
------

### Dependenies (Linux)

* python-setuptools
* easy_install  
* pip

### Set Up
``` bash
pip install virtualenv
 cd /path/to/umbrella
 virtualenv .
source bin/activate
pip install -r requirements.txt 

```
